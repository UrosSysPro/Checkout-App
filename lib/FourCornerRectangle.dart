import 'package:flutter/material.dart';

class FourCornerRectangle extends StatelessWidget {
  const FourCornerRectangle({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}